# Database (SQLite default — change provider in prisma/schema.prisma for PostgreSQL)
DATABASE_URL="file:./dev.db"

# For PostgreSQL (uncomment and update prisma/schema.prisma provider to "postgresql"):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/openclaw_cc?schema=public"

# Auth
NEXTAUTH_SECRET="generate-a-secret-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Encryption key for OpenClaw tokens (32 hex chars)
ENCRYPTION_KEY="0123456789abcdef0123456789abcdef"

# OpenClaw webhook verification
OPENCLAW_WEBHOOK_SECRET="your-webhook-secret-here"

# OpenClaw defaults (optional)
OPENCLAW_DEFAULT_MODE="mock"
OPENCLAW_DEFAULT_BASE_URL="http://localhost:8787"
OPENCLAW_DEFAULT_HEALTH_PATH="/health"
